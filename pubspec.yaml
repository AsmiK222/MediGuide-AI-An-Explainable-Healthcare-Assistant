name: mediguide_ai
description: MediGuide AI - D10
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.2.0
  image_picker: ^1.0.7
  image_picker_web: ^4.0.0
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.6
  camera: ^0.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true